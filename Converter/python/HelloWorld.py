class HelloWorld():
	def __init__(self, i=None, l=None, d=None, test=None):
		self.z = 3
		self.i = 0
		self.x = 0
		print 'Hello World!' 
		#test comment
if(i is not None):
		print 'Constructor with input.' 
if(i is None and l is not None and d is not None):
		print 'Second constructor with same input type' 
if(i is None and l is None and d is not None and test is not None):
		print 'Constructor with two inputs and variables.' 
		instance_variable = 0
	def testFunction(test,testString, ):
		print 'Testing' 
		#function test
	def testIndexFunction():
